apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-authenticated
  namespace: todoflow
spec:
  selector:
    matchLabels:
      app: command-service
  action: ALLOW
  rules:
    - from:
        - source:
            requestPrincipals: ["*"]
