apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.projectionWorker.name }}
spec:
  selector:
    app: {{ .Values.projectionWorker.name }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.projectionWorker.service.port }}
